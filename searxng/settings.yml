# SearXNG local, usado só pelo Forja (rede interna do compose; não exposto no host).
use_default_settings: true
server:
  secret_key: "forja-local-somente-rede-interna"
  limiter: false
  image_proxy: false
search:
  formats:
    - html
    - json
